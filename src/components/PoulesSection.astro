---
const poules = [
  {
    name: "Poule Terre",
    element: "Earth",
    icon: "🏔️",
    bg: "bg-emerald-50",
    accentBg: "bg-[#0A4A2E]",
    traits: ["Stable", "Naturel", "Ancré"],
    teams: 5,
    players: 30,
  },
  {
    name: "Poule Air",
    element: "Air",
    icon: "💨",
    bg: "bg-sky-50",
    accentBg: "bg-[#10305D]",
    traits: ["Léger", "Fluide", "Lumineux"],
    teams: 5,
    players: 30,
  },
  {
    name: "Poule Eau",
    element: "Water",
    icon: "💧",
    bg: "bg-blue-50",
    accentBg: "bg-[#142540]",
    traits: ["Fluide", "Adaptable", "Profond"],
    teams: 5,
    players: 30,
  },
];
---

<div id="poules" class="w-full py-20 px-4 bg-gradient-to-b from-white to-[#FFFEF8]">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-4xl md:text-5xl font-bold text-center mb-4 text-[#242424] animate-fade-in">
      Trois Éléments, Un Championnat
    </h2>
    <p class="text-center text-lg text-[#242424]/70 mb-16 animate-fade-in animation-delay-100 max-w-2xl mx-auto">
      Chaque poule incarne une force élémentaire unique. Quel élément régnera en maître ?
    </p>

    <div class="grid md:grid-cols-3 gap-8 mb-20">
      {poules.map((poule, index) => (
        <div
          class={`${poule.bg} rounded-3xl p-8 hover:shadow-xl transition-all duration-300 hover:-translate-y-2 animate-fade-in`}
          style={`animation-delay: ${(index + 1) * 150}ms;`}
        >
          <div
            class={`${poule.accentBg} w-16 h-16 rounded-2xl flex items-center justify-center text-2xl mb-6 hover:scale-110 transition-transform duration-300`}
          >
            {poule.icon}
          </div>
          <h3 class="text-2xl font-bold text-[#242424] mb-2">{poule.name}</h3>
          <p class="text-[#242424]/70 font-semibold mb-4">{poule.element}</p>
          <div class="flex justify-between text-[#242424]/70 text-sm mb-6">
            <span>{poule.teams} Équipes - {poule.players} Joueurs</span>
          </div>
          <div class="flex flex-wrap gap-2">
            {poule.traits.map((trait) => (
                <span class="text-sm text-[#242424]/70 px-3 py-1 bg-white/50 rounded-full">
                {trait}
              </span>
            ))}
          </div>
        </div>
      ))}
    </div>

    <div class="bg-gradient-to-r from-[#10305D] to-[#0A4A2E] rounded-3xl p-12 text-white">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
        {[
          { value: "15", label: "Équipes" },
          { value: "90", label: "Joueurs" },
          { value: "3", label: "Poules" },
          { value: "1", label: "Champion" },
        ].map((stat, index) => (
          <div class="animate-fade-in" style={`animation-delay: ${(index + 1) * 100}ms;`}>
            <div class="text-4xl md:text-5xl font-bold mb-2">{stat.value}</div>
            <div class="text-[#FFFEF8]/70">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.6s ease-out forwards;
    opacity: 0;
  }

  .animation-delay-100 {
    animation-delay: 0.1s;
  }
</style>
